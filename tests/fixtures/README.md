# Test fixtures

Every file in this directory is a small, synthetic representation of an official source structure. Values are deliberately non-publishable and are labeled `fixture` by the pipeline.

Fixtures test parsing, normalization, validation, database loading, and provenance behavior without relying on live network access or credentials.
